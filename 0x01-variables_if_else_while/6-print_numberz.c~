#include <stdio.h>
/*
*main - Print single digits of base 10
*
*Return: Always 0 (Success)
*/
int main(void)
{
int b;
for (b = 0, b < 10, b++)
{
putchar(x + '0');
if (b < 9)
{
putchar(',');
putchar();
}
}
puchar('\n');
return 0;
}
