#include "main.h"
/**
*_abs - checks code to give absolute value of integer
*
*@n: n is an integer
*
*Return: Always n (Success)
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
