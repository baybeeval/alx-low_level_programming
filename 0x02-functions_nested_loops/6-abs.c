#include "main.h"
/**
*abs - checks code
*
*
*Return: Always 0 (Success)
*/
int _abs(int)
{
int n;
{
if (n < 0)
printf("The absolute value of n is %d\n",abs(-1));
return (0);
}
else if (n > 0)
{
printf("The absolute value of n is %d\n",abs(1));
return (0);
}
else
{
printf("The absolute value of n is %d\n",abs(0));
}
return (0);
}
