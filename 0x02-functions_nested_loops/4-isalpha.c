#include "main.h"
/**
*isalpha - checks the alphabet
*
*Return: Always 1 else return 0
*
*/
int _isalpha(int c)
{
if(c < 97; c <= 122; c++)
{
return (1);
}
else if (C < 65; C <= 90; C++)
{
return (1);
}
else
{
return (0);
}
