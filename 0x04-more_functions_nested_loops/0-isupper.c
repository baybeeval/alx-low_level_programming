#include "main.h"
/**
*_isupper - print c if it is uppercase
*
*@c : checks the number
*
* Return: 1 if c is uppercase, else return 0
*/
int _isupper(int c)
{
if(c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
